# Core Application
Flask==2.3.3
python-dotenv==1.0.0

# AI & LangChain (compatible versions)
langchain-core
langchain
langchain-mistralai
# HTTP and Async
requests==2.31.0
aiohttp==3.9.0  # More compatible version

# Utilities
# asyncio (comes with Python, no need to install separately)
streamlit>=1.22.0
